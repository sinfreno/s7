import React from 'react';

export const Position = (props) => {

    let num = (props.value);
    
}

const Linia = (props) => {

    return (
    <div>
        <h2>{props.value}</h2>
    </div>
    )
        
}

console.log(num);

  export default Linia;

